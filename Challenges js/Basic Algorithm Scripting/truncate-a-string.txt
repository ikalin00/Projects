function truncateString(str, num) {
  let tuncate
  if(str.length>num){
   tuncate = str.slice(0,num) + "..."
   console.log(tuncate)
   return tuncate;
  }
  else
  return str
  
}

truncateString("A-tisket a-tasket A green and yellow basket", 8);